# In local dev this can stay unset — vite.config.ts proxies /api -> http://localhost:8000.
# In production, point this at the deployed backend (e.g. Render) directly.
VITE_API_BASE_URL=https://japanese-contract-translator.onrender.com
